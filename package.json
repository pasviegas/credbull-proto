{
  "name": "credbull",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format",
    "dev": "turbo dev",
    "postinstall": "husky install"
  },
  "packageManager": "yarn@4.0.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "turbo": "^1.11.2"
  }
}
